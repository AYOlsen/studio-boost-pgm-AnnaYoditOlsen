{% macro hero(title, category, description, h) %}
    <article class="hero">
        <div class="hero-img">
                <img src="../../images/{{ h }}"/>
        </div>
        <div class="hero-text">
            <p>{{ category }}</p>
            <h1>{{ title }}</h1>
            <p>{{ description }}</p>
        </div>
    </article>
{% endmacro%}
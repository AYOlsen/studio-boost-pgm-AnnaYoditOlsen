<article class="hero">
    <div class="hero-img">
            <img src="../../images/projects/{{ hero }}"/>
    </div>
    <div class="hero-text">
        <p>{{ category }}</p>
        <h1>{{ title }}</h1>
        <p>{{ description }}</p>
    </div>
    {# <div class="socials">
            {% for social in socials%}
                <a >
                    <img src="{{ social.img }}" />
                </a>
            {% endfor %}
        </div> #}
</article>
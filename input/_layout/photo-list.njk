{% extends "./base.njk" %}

{% set mode = "light" %}

{% block main%}
    {% include "../_partials/_banner.njk" %}

    <div class="collage max-width">
        {% for item in collections[iterator] %}
            <div class="card">
                <a href="{{ item.page.url }}">
                {% if collections[iterator] == collections.blog %}
                    {% include "../_partials/_blogphoto.njk" %}
                    {% else %}
                    <p class="card__title">{{ item.data.category }} - {{ item.data.title }}</p>
                    <img src="../images/{{ [iterator] }}/{{ item.data.image }}" />
                {% endif %}
                </a>
            </div>
        {% endfor %}
    </div>
{% endblock main %}
